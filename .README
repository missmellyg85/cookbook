## To set up for the first time
1. Run $ npm install
2. Run $ npm install -g webpack
3. Run $ npm install -g webpack-dev-server

## To compile the app
$ webpack

## To run the frontend app
$ npm start

## To run the backend app
$ ./gradlew clean bootRun
